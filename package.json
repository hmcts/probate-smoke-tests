{
  "name": "probate-smoke-tests",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "mocha test/healthcheck.js --timeout 15000 --reporter spec",
    "test-service": "mocha test/test-service.js --timeout 15000 --reporter spec"
  },
  "dependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "dotenv": "^2.0.0",
    "mocha": "^3.0.0",
    "request": "^2.74.0",
    "superagent-proxy": "^1.0.1"    
  }
}
